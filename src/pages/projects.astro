---
import BaseLayout from '../layouts/BaseLayout.astro'
import { projects } from '../data/projects'
---

<BaseLayout
  title="Projects"
  description="Selected projects with outcomes and stacks used. More case studies coming soon."
>
  <div class="flex flex-col gap-6">
    <div>
      <p class="text-sm font-medium uppercase tracking-[0.2em] text-slate-500">Projects</p>
      <h1 class="mt-2 text-4xl font-semibold tracking-tight text-slate-900">Recent work</h1>
      <p class="mt-2 max-w-2xl text-base text-slate-600">
        A few highlights. I’ll keep this list updated as I add more case studies.
      </p>
    </div>

    <div class="grid gap-4 sm:grid-cols-2">
      {projects.map((project) => (
        <article class="group rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_8px_30px_rgba(15,23,42,0.08)] transition hover:-translate-y-[2px] hover:shadow-lg">
          <div class="flex items-center justify-between gap-2">
            <h3 class="text-xl font-semibold text-slate-900">{project.name}</h3>
            <span class="text-xs font-semibold uppercase tracking-wide text-blue-600">Case</span>
          </div>
          <p class="mt-3 text-base text-slate-600">{project.summary}</p>
          <div class="mt-4 flex flex-wrap gap-2">
            {project.tech.map((tag) => (
              <span class="rounded-full border border-slate-200 px-3 py-1 text-xs font-semibold text-slate-600">
                {tag}
              </span>
            ))}
          </div>
          <a
            class="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-blue-600 transition group-hover:translate-x-[2px]"
            href={project.link}
          >
            View project
            <span aria-hidden>→</span>
          </a>
        </article>
      ))}
    </div>
  </div>
</BaseLayout>
