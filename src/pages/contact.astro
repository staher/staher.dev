---
import BaseLayout from '../layouts/BaseLayout.astro'
import { contactLinks } from '../data/site'
---

<BaseLayout
  title="Contact"
  description="Get in touch with Taher via email or social links. Collaboration and conversations welcome."
>
  <div class="flex flex-col gap-6">
    <div>
      <p class="text-sm font-medium uppercase tracking-[0.2em] text-slate-500">Contact</p>
      <h1 class="mt-2 text-4xl font-semibold tracking-tight text-slate-900">Say hello</h1>
      <p class="mt-2 max-w-2xl text-base text-slate-600">
        Want to collaborate or chat? Reach out via email or find me on the platforms below. I’ll add
        a résumé link here soon.
      </p>
    </div>
    <div class="flex flex-wrap gap-3">
      {contactLinks.map((link) => (
        <a
          href={link.href}
          class="rounded-full border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-900 transition hover:-translate-y-[1px] hover:bg-white hover:shadow-sm"
        >
          {link.label}
        </a>
      ))}
    </div>
  </div>
</BaseLayout>
